# Django 4.2 LTS is the minimum version that supports Python 3.12.
# Django 3.1.x is EOL and incompatible with Python 3.12.
Django==4.2.13
asgiref==3.8.1
sqlparse==0.5.0

# django-crispy-forms 2.x split Bootstrap template packs into separate
# packages. crispy-bootstrap4 is required when CRISPY_TEMPLATE_PACK='bootstrap4'.
django-crispy-forms==2.1
crispy-bootstrap4==2024.1

# S3 storage backend
django-storages==1.14.2
boto3==1.34.69
botocore==1.34.69
s3transfer==0.10.1
jmespath==1.0.1

# WSGI server — replaces runserver for production use
gunicorn==21.2.0

# Database driver — 2.2.x builds cleanly against Ubuntu 24.04's libmysqlclient
mysqlclient==2.2.4

# Image handling — Pillow 8.x is incompatible with Python 3.12
Pillow==10.3.0

# Utilities
python-decouple==3.8
python-dateutil==2.9.0
pytz==2024.1
six==1.16.0
urllib3==2.2.1
